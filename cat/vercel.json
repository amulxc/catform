{
    "version": 2,
    "builds": [{
        "src": "cat/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "cat/wsgi.py"
        }
    ]
}